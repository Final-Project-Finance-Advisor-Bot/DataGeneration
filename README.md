# DataGeneration
 Infrastructure for synthetic labelled data generation used to support intent classification research within the Financial Advisor Bot system.  This repository will host tooling responsible for producing structured JSONL datasets for supervised training experiments using locally hosted language models.
